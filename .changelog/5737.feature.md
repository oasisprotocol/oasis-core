go/runtime: Periodically clean up cached bundles directory

Up until now the cached exploded bundles were never removed, which could
result in a lot of disk usage after a few upgrades.

From now on, we are removing them automatically:

1. At initialization time, for bundles no longer present in the configuration
   file (both regular and detached).
