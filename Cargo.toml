[workspace]
members = [
    "runtime",
    "runtime-loader",
    "client",
    "keymanager-client",
    "keymanager-api-common",
    "keymanager-lib",
    "tools",

    # Test runtimes.
    "tests/runtimes/simple-keyvalue",
    "tests/runtimes/simple-keymanager",
]

[profile.release]
panic = "abort"
