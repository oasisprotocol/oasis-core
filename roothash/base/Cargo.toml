[package]
name = "ekiden-roothash-base"
version = "0.2.0-alpha"
authors = ["Oasis Labs Inc. <info@oasislabs.com>"]
description = "Ekiden root hash interface traits"
keywords = ["ekiden"]
repository = "https://github.com/oasislabs/ekiden"

[dependencies]
ekiden-common = { path = "../../common", version = "0.2.0-alpha" }
ekiden-storage-base = { path = "../../storage/base", version = "0.2.0-alpha" }
serde = "1.0.71"
serde_derive = "1.0"
serde_cbor = { git = "https://github.com/oasislabs/cbor", tag = "v0.9.0-ekiden1" }
serde_bytes = "~0.10"

[target.'cfg(not(target_env = "sgx"))'.dependencies]
ekiden-roothash-api = { path = "../api", version = "0.2.0-alpha" }
