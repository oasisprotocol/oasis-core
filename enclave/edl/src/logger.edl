enclave {
    untrusted {
        /**
         * Relays the given EkidenLoggerRecord from the enclave to the
         * untrusted log crate.
         */
        void untrusted_log(
            [in, size=record_length] const uint8_t *record,
            size_t record_length
        );
    };
};
