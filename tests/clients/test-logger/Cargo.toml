[package]
name = "test-logger-client"
version = "0.2.0-alpha"
authors = ["Oasis Labs Inc. <info@oasislabs.com>"]
description = "Ekiden logger test"

[features]
default = []

[dependencies]
client-utils = { path = "../utils" }
ekiden-core = { path = "../../../core/common" }
ekiden-runtime-client = { path = "../../../runtime/client" }
ekiden-rpc-client = { path = "../../../rpc/client" }
test-logger-api = { path = "../../runtimes/logger/api" }
clap = "2.29.1"

