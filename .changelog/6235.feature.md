go/consensus/cometbft: Add stateless client node

One can now start a stateless client as follows:

```yaml
mode: client-stateless
# ... sections not relevant are omitted ...
consensus:
    providers:
        - <node-address-1>
        - <node-address-2>
        # Add more node addresses as needed
```
