[package]
name = "ekiden-trusted"
version = "0.2.0-alpha"
authors = ["Oasis Labs Inc. <info@oasislabs.com>"]
description = "Ekiden trusted library"
keywords = ["ekiden"]
repository = "https://github.com/ekiden/ekiden"

[dependencies]
ekiden-enclave-trusted = { path = "../../enclave/trusted", version = "0.2.0-alpha" }
ekiden-rpc-trusted = { path = "../../rpc/trusted", version = "0.2.0-alpha" }
ekiden-db-trusted = { path = "../../db/trusted", version = "0.2.0-alpha" }
ekiden-contract-trusted = { path = "../../contract/trusted", version = "0.2.0-alpha" }
ekiden-key-manager-client = { path = "../../contracts/key-manager/client", version = "0.2.0-alpha" }
