go/oasis-node/cmd/storage: Release offline pruning command

Command `oasis storage prune-experimental` has been
released as `oasis-storage prune` and now in addition
to consensus databases also prunes runtime databases.
